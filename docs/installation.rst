============
Installation
============

At the command line::

    $ easy_install tweetsearch

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tweetsearch
    $ pip install tweetsearch
